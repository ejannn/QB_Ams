/*
  QRBAMS DATABASE MIGRATION BASELINE

  The current database schema already existed
  before migration tracking was introduced.

  Future database changes will be added as
  numbered migration files.
*/

SELECT 1;